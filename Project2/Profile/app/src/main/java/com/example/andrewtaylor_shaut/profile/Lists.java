package com.example.andrewtaylor_shaut.profile;

import android.app.Activity;
import android.os.Bundle;

public class Lists extends Activity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_lists);
    }
    //make this a list of lists like flowers app
    //have this file have a switch that depends on whether I click the lists or twitter button on the
        //previous page as for what data to load (for twitter it will load here and not use the list child activity
}
